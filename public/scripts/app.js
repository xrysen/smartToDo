/* $(() => {
  $.ajax({
    method: "GET",
    url: "/api/users"
  }).done((users) => {
    for(user of users) {
      $("<div>").text(user.name).appendTo($("body"));
    }
  });;

  $.ajax({
    method: "POST",
    url: "/api/tasks"
  }).done((newTask) => {
    // TODO:: add new task to list of tasks
  });;
}); */

